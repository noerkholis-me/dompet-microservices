## 2024-05-23 - Navigation Smoothness
**Learning:** Using `<a>` tags for internal navigation in an SPA destroys the user experience by causing full page reloads, making the app feel slow and "heavy".
**Action:** Always audit navigation elements and enforce `Link` component usage for internal routes to ensure instant transitions.
